apiVersion: opentelemetry.io/v1alpha1
kind: OpenTelemetryCollector
metadata:
  name: otel
spec:
  config: |
    receivers:
      otlp:
        protocols:
          grpc:
          http:
    processors:

    exporters:
      logging:

      jaeger:
        endpoint: simplest-collector:14250

    service:
      pipelines:
        traces:
          receivers: [otlp]
          processors: []
          exporters: [logging]